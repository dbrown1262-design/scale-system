


I need a harvest summary report (Harvest/HarvestSummary.py) that contains the following columns:

Strain
Wet Weight
Dry Weight
Dry Percent (Dry / Wet)
Flower Weight
Flower Percent (Flower / Dry)
Smalls Weight
Smalls Percent (Smalls / Dry)
Trim Weight
Trim Percent (Trim / Dry)
Stems (Dryweight - flower - smalls - trim)
Stems Percent (Stems / Dry)

Use LoadCrops to populate a Crop combo box
When a Crop is selected:
   use SubSupa.GetStrainWeights(CropNo) to retrieve dict with strain, wet and dry weights
   for each strain returned  use subsupa GetBuckedSummary(CropNo, Strain) to retrieve total weight for Flower, Smalls and Trim



Our system includes a VEVOR ice maker, a mini OSPREY washer and a water chiller.  We use Harvestrite freeze driers.
for frozen plant material, we do not add ice unless the water temperature (as measured by portable IR thermometer)
We currently do 5 10 minute washes starting with 170 RPM, then 185 then three at 200.  For each wash, the hash is put into the trays that came with the freeze dryers.  Plant matter is weighed before washing and hash is weighted after freeze drying

in printbudkedreport:
SubSupa.LoadToteReport now takes CropNo, Strain parameters.  Change screen- remove start and end dates, get CropNo and Strain instead.  Add TotType column to treeview



ubibot notes:

UbiBot Account	
email- service@adkhempco.com
Password- stored in google account dbrown1262@verison.net
Setup Password Adk1891
User Id- AdkHempCo

PI setup
download imager_2.0.7.exe, put in scale/Amin folder
if not installed, run the exe, imager will be installer
run Imager:
device- Pi 4
version- OS Lite(64 bit)
name- nas-monitor
User name- adkadmin
password- Adk1891
flash the memory card, put in the Pi
plug in network cable, plug in pi (USB power cord)
ping nas-monitor from workstation command prompt
ssh adkadmin@address from ping
update software:
   sudo apt update && sudo apt upgrade -y
install python
sudo apt install python3-pip -y
sudo apt install python3-venv -y
python3 -m venv ~/nas-env
source ~/nas-env/bin/activate
pip3 install requests
ssh adkadmin@nas-monitor

scp "C:\Users\Adk\Documents\scale\Common\NASmonitor.py" adkadmin@192.168.1.126:~
source ~/nas-env/bin/activate
pip install supabase
python ~/NASmonitor.py
sudo nano /etc/systemd/system/nas-monitor.service
Paste:
[Unit]
Description=NAS Monitor Python Service
After=network-online.target
Wants=network-online.target

[Service]
Type=simple
User=adkadmin
WorkingDirectory=/home/adkadmin
ExecStart=/home/adkadmin/nas-env/bin/python /home/adkadmin/NASmonitor.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target

Ctrl+O to save, press enter, Ctrl+X to exit

sudo systemctl daemon-reload
sudo systemctl enable nas-monitor.service
sudo systemctl start nas-monitor.service
sudo systemctl status nas-monitor.service

checks:

sudo systemctl restart nas-monitor.service
journalctl -u nas-monitor.service -f
sudo reboot
ssh adkadmin@nas-monitor
sudo systemctl status nas-monitor.service


update NasMonitor.py
from control panel workstation (not ssh to nas-monitor)
scp "C:\Users\Adk\Documents\scale\Common\NASmonitor.py" adkadmin@192.168.1.126:~
p/w Adk1891

sudo systemctl restart nas-monitor.service

journalctl -u nas-monitor.service -f

Finding 1: Licensee provided security system “Corrective Actions Implemented” in email on 4/6/2026, however, additional information is needed. Please confirm the following:
• The ability for the video surveillance system to remain operational during a power outage for a minimum of eight (8) hours
• 90-day video recording retention for sampling events
• A failure notification system that provides an audible, text, or visual notification of any failure in the system to a designated representative within five (5) minutes of the failure, either by telephone, email, or text message
This finding has not been satisfied in the CAP and remains open.


NAS summary:

we initially installed a ups on the NAS, but it did not have the capacity to run for 8 hours.

We replaced the UPS with an EcoFlow DELTA 2 Power Station.  This unit is designed for longer run times.

We also purchased and Anker SOLIX C300 power station to run our Spectrum modem and router

We implemented a NAS monitor using a Raspberry Pi computer.  The monitor checks the NAS every sixty seconds and updates a (Cloud based) Supabase status table .  If it fails to connect for three consecutive checks, it sends a warning email and logs the event in a Supabase log table.  When the NAS is back up, it sends an email and logs the event.  Finally, the monitor sends a "heartbeat" email every day to confirm the system is running.

We also implemented a log viewer that allows staff to view the current status of the NAS and the message log.

We tested the system by unplugging both power stations for 8 hours.  The EcoFlow unit had 73% of its battery capacity and estimated that it could run an additional 21 hours at the current discharge rate.  The SOLIX power station was still running and had 19% of its battery capacity.

With regards to the requirement for retaining sampling videos, we do not rely on the NAS: we download the video and store it on a google drive, where it can remain until we delete it.  